# CUPS authenticates print users against LDAP via SSSD only.
auth       sufficient   pam_sss.so
auth       required     pam_deny.so

account    sufficient   pam_sss.so
account    required     pam_deny.so
